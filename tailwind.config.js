/**
 * Tailwind CSS v4 Configuration — Di Catet
 * ──────────────────────────────────────────
 * Neo-Brutalism × Soft-Glass Design System
 *
 * All theme tokens are defined via @theme directive in
 * resources/css/app.css (Tailwind v4 convention).
 *
 * Palette:
 *   obsidian    #0A0A0A   (background)
 *   surface     #141414   (elevated bg)
 *   card        #1A1A1A   (card bg)
 *   acid        #D4FF00   (income / primary / CTA)
 *   purple      #CF9FFF   (expense / secondary)
 *   text-main   #E8E8E8
 *   text-sub    #6B6B6B
 *   border      #2A2A2A
 *
 * Fonts:
 *   display     Space Grotesk (headings, lowercase)
 *   sans        Inter (body)
 *   mono        JetBrains Mono (numbers, amounts)
 */
export default {
  content: [
    './resources/**/*.blade.php',
    './resources/**/*.js',
    './resources/**/*.vue',
  ],
};
